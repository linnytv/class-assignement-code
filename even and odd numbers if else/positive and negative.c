#include <stdio.h>
#include <stdlib.h>

 int number;
  printf(" Enter Number: \n");
  scanf("%d", &number);
  if (number%2 !=0) {
    printf("odd number");
  }
  else {
    printf("even number");
  }
    return 0;
